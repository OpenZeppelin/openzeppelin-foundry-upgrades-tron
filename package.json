{
  "name": "@openzeppelin/foundry-upgrades-tron",
  "version": "0.1.0",
  "description": "Foundry library for deploying and managing upgradeable contracts on TRON",
  "license": "MIT",
  "files": [
    "src/**/*",
    "rpc/**/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/OpenZeppelin/openzeppelin-foundry-upgrades-tron.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "compile": "forge build",
    "test": "npm run test:solidity && npm run test:rpc",
    "test:solidity": "FOUNDRY_OUT=out FOUNDRY_PROFILE=default forge test -vvv --ffi --force",
    "test:validation": "FOUNDRY_OUT=out FOUNDRY_PROFILE=default forge test --match-path test/Validation.t.sol -vvv --ffi --force",
    "test:rpc": "node --test rpc/test/*.test.cjs",
    "lint": "prettier --log-level warn --ignore-path .gitignore '{src,test,rpc}/**/*.{sol,cjs}' --check && solhint 'src/**/*.sol'",
    "lint:fix": "prettier --log-level warn --ignore-path .gitignore '{src,test,rpc}/**/*.{sol,cjs}' --write",
    "rpc": "node rpc/cli.cjs"
  },
  "dependencies": {
    "ethers": "6.17.0",
    "tronweb": "6.4.0"
  },
  "devDependencies": {
    "@openzeppelin/upgrades-core": "1.46.0",
    "prettier": "3.9.5",
    "prettier-plugin-solidity": "2.3.1",
    "solhint": "6.2.3"
  },
  "peerDependencies": {
    "@openzeppelin/upgrades-core": "^1.46.0"
  }
}
